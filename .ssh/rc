#!/bin/bash
# Symlink the SSH agent socket to a stable path so tmux sessions survive reconnects
if [ -n "$SSH_AUTH_SOCK" ] && [ "$SSH_AUTH_SOCK" != "$HOME/.ssh/agent_sock" ]; then
    ln -sf "$SSH_AUTH_SOCK" "$HOME/.ssh/agent_sock"
fi
